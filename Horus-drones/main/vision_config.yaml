provenance:
  tuned_from: [bench_20260805_151709, flight2_20260805_155954]
  tuned_on: 2026-08-06
  tuned_by: analysis/replay_cage.py
  retargeted_on: 2026-09-04
  note: >
    Field names match cage_detector.CageParams exactly; anything not listed
    here keeps the dataclass default. Section names are cosmetic.
    Everything outside the color section still comes from the 2026-08-06
    tuning run: bench held 94.5% of frames with the cage whole on 96.7% of
    those and a 24 px median centroid step. The color section was retargeted
    from the old green markers to #c74026 and has not been re-measured on a
    recording yet.

color:
  hue_low: 172
  hue_high: 18
  saturation_low: 60
  value_low: 30
  core_saturation: 120
  core_value: 60
  fallback_frames: 5

morphology:
  open_kernel: 1
  close_kernel: 0

corners:
  min_corner_area: 2
  min_area_fraction: 0.20
  max_corner_area: 4000
  min_corner_fill: 0.30
  max_corner_aspect: 3.5
  shape_test_min_area: 20
  max_corners: 32

grouping:
  neighbours: 8
  link_reach: 12.0
  link_spacing: 2.5
  max_radius_ratio: 4.0

accept:
  min_corners_per_cage: 2
  min_cluster_area: 8
  min_quality: 0.10
  max_range_m: 15.0

tracking:
  continuity_bonus: 1.4
  continuity_reach: 3.0
  continuity_hold: 10
  span_floor_reach: 1.5

roi:
  roi_margin_px: 60
  roi_margin_frac: 0.4
  roi_rescan_frames: 10
